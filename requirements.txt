# Pinned for Streamlit Community Cloud, which builds on Python 3.14.
# Every pin here must ship a cp314 wheel. numpy < 2.3.4 does not, and uv
# falling back to a source build is what hung the first deploy.
streamlit==1.45.1
pandas==2.3.3
numpy==2.3.4
altair==5.5.0
rapidfuzz==3.14.3
